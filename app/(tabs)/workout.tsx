import { View, Text } from "react-native";

export default function WorkoutScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Workout Tracker</Text>
        </View>
    );
}
